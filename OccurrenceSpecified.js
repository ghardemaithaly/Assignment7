//index Of() Method is used to specify check the occurrence of a text in a string.

const statement='I am aStudent of iNeuron';
const search='iNeuron';
const indexoffirst = statement.indexOf(search)
console.log(`the index of 1st "${search}" from strating is ${indexoffirst}`);